# Healthcare Portal (MVP)

## Backend
cd backend
cp .env.example .env
# edit .env
npm install
npm run dev

## Frontend
cd frontend
cp .env.example .env
npm install
npm start

## Using docker-compose (dev)
docker-compose up --build
